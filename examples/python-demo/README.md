# RepoRacer Python Demo

Small Python project shape for trying RepoRacer against a standard-library test suite.

```bash
python -B -m unittest discover -s tests
npx reporacer init
npx reporacer run --agents fake-success,fake-noop --tasks 1
```
